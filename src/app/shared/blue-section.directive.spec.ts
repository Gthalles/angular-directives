import { BlueSectionDirective } from './blue-section.directive';

describe('BlueSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueSectionDirective();
    expect(directive).toBeTruthy();
  });
});
