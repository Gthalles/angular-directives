import { HighlightMouse2Directive } from './highlight-mouse2.directive';

describe('HighlightMouse2Directive', () => {
  it('should create an instance', () => {
    const directive = new HighlightMouse2Directive();
    expect(directive).toBeTruthy();
  });
});
